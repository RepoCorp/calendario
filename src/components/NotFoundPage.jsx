export default function NotFoundPage() {
  return (
    <main className="page shell">
      <h1>Page not found</h1>
      <p>That URL does not match a configured Advent day.</p>
      <a href="/">Back to calendar</a>
    </main>
  );
}
